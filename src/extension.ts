import * as vscode from 'vscode';

/**
 * Extension entry point — Engineering Workspace.
 *
 * Activates on startup finished. Registers the webview view provider,
 * chat participant, and language model tools.
 *
 * This is a minimal stub for Task 1 (Project Scaffold). Full
 * implementation arrives in Tasks 11–13.
 */
export function activate(_context: vscode.ExtensionContext): void {
  // Stub — real registration in Tasks 11–13
}

export function deactivate(): void {
  // Stub — cleanup in Tasks 11–13
}
